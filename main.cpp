#include <iostream>

int main()
{
    using namespace std;
    int a, b, sum,del;
    cout << "Enter a, b: ";
    cin >> a >> b;
    sum = a + b; 
    del=a/b;
    cout << "Sum: " << sum;
     cout << "del: " << del;
    return 0; 
}
